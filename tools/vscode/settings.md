[toc]

# settings

## Python

```json
{
    "python.pythonPath": "/Users/qiubing/anaconda3/bin/python",  # 执行路径
    "python.linting.pylintEnabled": true,  # 开启 pylint 代码风格规范检测
    "python.linting.flake8Enabled": true,  # 开启 flake8 代码风格规范检测
    "python.formatting.blackPath": "black",  # 指定 black 执行路径（代码格式化工具）
    "python.formatting.blackArgs": ["--line-length=80"],  # 设置每行长度为 80
    "editor.formatOnSave": true  # 保存时自动格式化代码
}
```

